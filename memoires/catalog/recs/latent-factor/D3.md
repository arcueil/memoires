# ✓/✗ D3 · for a latent-Gaussian model → **NCP** works (and is necessary) when the likelihood is **non-Gaussian** so marginalizatio

[Latent-factor models (factor analysis / SEM / IRT / PPCA / latent-Gaussian)](../../pages/latent-factor.md)

**Why this holds — the governing claim:**

- ↑ [latent-factor C4 · For latent-Gaussian models, marginalizing the latent field strictly dominates no](../../claims/latent-factor/C4.md)

**✓/✗ D3**  · for a latent-Gaussian model → **NCP** works (and is necessary) when the likelihood is
**non-Gaussian** so marginalization is only approximate; it does **NOT** match marginalization for the
**conjugate Gaussian** class (an unnecessary fallback that reshapes, rather than removes, the funnel).
- why: NCP and marginalization are not equivalent — NCP reshapes the funnel while marginalization
  removes it; with exact conjugacy the marginal route dominates, but without it NCP may be the only
  option.
- conditions: non-Gaussian likelihood → marginalization approximate → NCP competitive/necessary; conjugate Gaussian → prefer marginalization.
- tier: 🟢 · source: [dansblog:linear-mixed-effects](https://dansblog.netlify.app/posts/2022-03-22-a-linear-mixed-effects-model/a-linear-mixed-effects-model.html)
- efficacy: {divergences: pending · min_ess: pending · ess_per_sec: pending · rmse: pending · coverage: pending}
- moves: "Treat the NCP regression as evidence, not noise — check whether NCP is even the right regime and whether the code is correct" · "Flip the global parameterization and compare which warnings appear" · "Classify whether the distribution to be reparameterized is a likelihood (data) or a hierarchical prior (parameter)"


## Related across the catalog

*Similar challenges in other model classes / computation areas (embedding neighbors):*

- [✓ gaussian-process G5 · for an **NCP GP with a Gaussian likelihood linear in the latent** → a ](../../recs/gaussian-process/G5.md) `0.90`
- [✓ gaussian-process S4 · for a **sparse-precision latent-Gaussian model** → **parameterizing vi](../../recs/gaussian-process/S4.md) `0.87`
- [✓ gaussian-process G3 · for a **latent-Gaussian model** y = Au + ε, u ~ N(0,Q(θ)⁻¹) → **margin](../../recs/gaussian-process/G3.md) `0.85`
- [✓ time-series-state-space D2 · for the same → **marginalize the latent states via the forward algorit](../../recs/time-series-state-space/D2.md) `0.84`
