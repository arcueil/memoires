# ✓ S4 · for a **sparse-precision latent-Gaussian model** → **parameterizing via the precision Q(θ), not the covariance Σ(θ)** wo

[Gaussian processes & latent-Gaussian models](../../pages/gaussian-process.md)

**Why this holds — the governing claim:**

- ↑ [gaussian-process C3 · An exact dense GP does not scale — the fix is a *structured* representation (low](../../claims/gaussian-process/C3.md)

**✓ S4**  · for a **sparse-precision latent-Gaussian model** → **parameterizing via the precision Q(θ),
not the covariance Σ(θ)** works (mechanistically necessary).
- why: the inverse of a connected sparse matrix is generically dense, so covariance form destroys all sparsity and collapses O(n^1.5) sparse Cholesky to O(n³).
- conditions: sparse prior precision (LMEs, AR, ICAR, splines, Markovian GPs); design matrix Z with ≤ O(√n) nonzeros per row.
- tier: 🟢 · source: [dansblog:lme](https://dansblog.netlify.app/posts/2022-03-22-a-linear-mixed-effects-model/a-linear-mixed-effects-model.html), [dansblog:sparse7](https://dansblog.netlify.app/posts/2022-11-27-sparse7/sparse7.html)
- efficacy: {divergences: pending · min_ess: pending · ess_per_sec: pending · rmse: pending · coverage: pending}
- moves: "Build the model on the precision Q(θ) directly — never form or invert the dense covariance Σ(θ), which destroys the sparsity"


## Related across the catalog

*Similar challenges in other model classes / computation areas (embedding neighbors):*

- [✓ spatial-areal S1 · for a latent-Gaussian spatial model y = Au + ε, u ~ N(0, Q(θ)⁻¹) with ](../../recs/spatial-areal/S1.md) `0.92`
- [spatial-areal C1 · Precision, not covariance, is the working representation for structure](../../claims/spatial-areal/C1.md) `0.89`
- [✓/✗ latent-factor D3 · for a latent-Gaussian model → **NCP** works (and is necessary) when th](../../recs/latent-factor/D3.md) `0.86`
- [✓ latent-factor D2 · for a **conjugate latent-Gaussian** model → **analytic marginalization](../../recs/latent-factor/D2.md) `0.86`
