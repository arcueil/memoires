# ✓/✗ F8 · for **marginalizing a sum of independent binomial** latent counts against an observed total in HMC → a **Poisson (or Neg

[Hierarchical / multilevel models](../../pages/hierarchical-multilevel.md)

**Why this holds — the governing claim:**

- ↑ [time-series-state-space C3 · Discrete latent states must be marginalized out — then the dominant failure is l](../../claims/time-series-state-space/C3.md)

**✓/✗ F8**  · for **marginalizing a sum of independent binomial** latent counts against an observed
total in HMC → a **Poisson (or NegBin) approximation works** when each success probability is small
relative to its count; it does **NOT** work when probabilities are large and counts small.
- why: exact marginalization is an O(count²) discrete convolution dominating runtime; the Poisson approx collapses it — but introduces dispersion error when p is large.
- conditions: gradient-based sampler; small p relative to count for the approximation; else use the exact convolution with the log_sum_exp running-prefix speedup.
- tier: 🟡 · source: [mc-stan:3012](https://discourse.mc-stan.org/t/a-more-efficient-implementation-of-the-sum-of-independent-binomials/3012)
- efficacy: {divergences: pending · min_ess: pending · ess_per_sec: pending · rmse: pending · coverage: pending}
- moves: "Reparameterize: split the count matrix into sub-matrices, estimate a per-row baseline μ and φ"


## Related across the catalog

*Similar challenges in other model classes / computation areas (embedding neighbors):*

- [✓/✗ mixture Z4 · for **marginalizing a sum of independent binomial latent counts agains](../../recs/mixture/Z4.md) `0.99`
- [✓ time-series-state-space D2 · for the same → **marginalize the latent states via the forward algorit](../../recs/time-series-state-space/D2.md) `0.83`
- [✓ CC-model-evaluation F8 · when you **marginalized a discrete latent z** out for HMC → recovering](../../recs/CC-model-evaluation/F8.md) `0.83`
- [✓ CC-geometry-sampling DM10 · when **discrete latent structure can be summed** (finite mixture, HMM)](../../recs/CC-geometry-sampling/DM10.md) `0.82`
