# ✗ G1 · when reading **`lp__` / `target` as the pointwise log-likelihood** for LOO/WAIC → does **NOT** work.

[Cross-cutting: Model evaluation — is the fit trustworthy, is the model adequate, and is the comparison honest?](../../pages/CC-model-evaluation.md)

**Why this holds — the governing claim:**

- ↑ [CC-model-evaluation C5 · Predictive model comparison ranks (never certifies), demands the correctly-facto](../../claims/CC-model-evaluation/C5.md)

**✗ G1**  · when reading **`lp__` / `target` as the pointwise log-likelihood** for LOO/WAIC → does
**NOT** work.
- why: `target +=` only increments the total log prob; nothing pointwise is saved. Recompute per-unit contributions in generated quantities (cheap: once per iteration, not autodiffed); the droppable-constant rules for the model-block target do NOT transfer.
- conditions: whenever the log_lik vector is assembled by hand.
- tier: 🟡 · source: [mc-stan:32502](https://discourse.mc-stan.org/t/extract-pointwise-log-likelihood-from-a-user-defined-stan-loglikelihood-function/32502), [mc-stan:11993](https://discourse.mc-stan.org/t/loo-model-selection-for-state-space-model/11993), [mc-stan:37577](https://discourse.mc-stan.org/t/how-to-deal-with-total-ordering-in-practice-under-label-switching/37577)
- efficacy: {divergences: pending · min_ess: pending · ess_per_sec: pending · rmse: pending · coverage: pending}
- moves: "contrast model block vs generated quantities" · "Compute the true log pointwise predictive density from scratch (average the likelihood over posterior draws per observation, then log and sum)"


## Related across the catalog

*Similar challenges in other model classes / computation areas (embedding neighbors):*

- [✓ regression PR3 · for a **log-link count model** → **eliciting slope priors in log space](../../recs/regression/PR3.md) `0.82`
- [✗ regression VS1 · for **selecting a smaller predictor set** → refitting each candidate s](../../recs/regression/VS1.md) `0.82`
- [✗ sparse-shrinkage V1 · for **selecting a smaller predictor set** → **scoring candidate submod](../../recs/sparse-shrinkage/V1.md) `0.81`
- [✓ CC-geometry-sampling DM1 · when a model has **discrete component-assignment variables z** → margi](../../recs/CC-geometry-sampling/DM1.md) `0.80`


## Technique (pymc-labs)

**PyMC log_likelihood API for LOO-CV** — PyMC v6 / ArviZ 1.x does NOT accept a top-level compute_log_likelihood= on pm.sample; request via pm.sample(idata_kwargs={'log_likelihood': True}) or compute post-hoc with pm.compute_log_likelihood(idata) before az.loo.
*Source: [pymc-labs:model-evaluation](https://github.com/pymc-labs/python-analytics-skills)*

**nutpie backend requires pm.compute_log_likelihood before az.compare / LOO** — 'PyMC's nutpie sampler does not store the pointwise log-likelihood by default; call pm.compute_log_likelihood(idata, model=...) for each model before az.compare / az.loo, or the comparison silently lacks the log_likelihood group.' Complements G1 with the concrete PyMC/nutpie gotcha and API.
*Source: [pymc-labs:model-evaluation](https://github.com/pymc-labs/python-analytics-skills)*
