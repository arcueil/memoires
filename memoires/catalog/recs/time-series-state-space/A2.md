# ✓ A2 · for a **hierarchical AR(1)** → the **mean-centered generative form** y_it = μ_i + δ·(y_{i,t−1}−μ_i) + ε_it with the **st

[Time-series & state-space models](../../pages/time-series-state-space.md)

**Why this holds — the governing claim:**

- ↑ [time-series-state-space C5 · The recursion is conditional — respect what it conditions on (initial condition;](../../claims/time-series-state-space/C5.md)

**✓ A2**  · for a **hierarchical AR(1)** → the **mean-centered generative form**
y_it = μ_i + δ·(y_{i,t−1}−μ_i) + ε_it with the **stationary initial condition**
Normal(μ_i, σ_e/√(1−δ²)) works.
- why: models the initial condition's marginal rather than conditioning on it, removing the correlation between the first observation and μ_i that biases δ.
- conditions: stationary AR(1), |δ|<1; unit-root/nonlinear processes need a different initial-condition treatment (same principle, different closed form).
- tier: 🟡 · source: [mc-stan:5136](https://discourse.mc-stan.org/t/dynamic-panel-data-models-with-stan/5136)
- efficacy: {divergences: pending · min_ess: pending · ess_per_sec: pending · rmse: pending · coverage: pending}
- moves: "Reframe to a latent continuous state-space: latent f[t] evolves as a Gaussian (V)AR of previous latent states + covariates + noise" · "Establish a validation harness: fit synthetic data with KNOWN parameters and check recovery" · "Choose the correct missing-data mechanism for the attrition"


## Related across the catalog

*Similar challenges in other model classes / computation areas (embedding neighbors):*

- [✓ hierarchical-multilevel F6 · for a **hierarchical AR(1)** → the **mean-centered lag** generative fo](../../recs/hierarchical-multilevel/F6.md) `0.96`
- [✓ regression PR5 · for a **high-dimensional scale-mixture (local-global) prior** → calibr](../../recs/regression/PR5.md) `0.79`
- [✗ hierarchical-multilevel D4 · for **residual hierarchical decompositions** (θ_l = θ + δ_l) → **count](../../recs/hierarchical-multilevel/D4.md) `0.78`
- [✓/✗ gaussian-process G4 · for a **GP regression** y ~ N(a+f, σ) with **divergences concentrated ](../../recs/gaussian-process/G4.md) `0.78`


## Technique (pymc-labs)

**Stationary initial condition via init_dist (AR-with-intercept)** — A2 requires the stationary initial condition Normal(mu, sigma/sqrt(1-delta^2)). PyMC-specific how: init_dist=pm.Normal.dist(c/(1-rho), sd) on pm.AR, with c/(1-rho) = stationary mean. CAUTION worth recording: the pymc example sets the init SD to plain `sigma`, NOT the stationary sigma/sqrt(1-rho^2) that A2/Nickell-bias fix demand -- so their example gets the mean right but under-specifies the initial variance.
*Source: [pymc-labs:time-series](https://github.com/pymc-labs/python-analytics-skills)*
