# C2 · The GP latent field is a high-dimensional funnel — whiten it (NCP Cholesky) or marginalize it out; the centered form fails *invisibly* 🟢

**↑ [SA4 · Posterior geometry and identifiability are structural; only ](../../SUPER_AXIOMS.md)** · [Gaussian processes & latent-Gaussian models](../../pages/gaussian-process.md)

**Statement.** The correlated latent field f is a banana/funnel geometry that HMC cannot traverse in the
centered parameterization — and it fails *silently* (no divergences, no tree-depth saturation, just
extreme E-FMI and huge R̂); the two working responses are the non-centered Cholesky whitening
f = L·f̃ with f̃ ~ N(0,I), or analytically marginalizing the field so MCMC runs only on the
low-dimensional hyperparameters.

**Nuance.** NCP whitening is *necessary* for HMC to work and *sufficient* to clear the geometry when
hyperparameters are fixed and the likelihood is relatively diffuse — regardless of observation model
(Gaussian or Poisson log-link) — because it lets a diagonal mass matrix approximate the posterior without
full Hessian information. Marginalization is the deeper move for latent-Gaussian models y = Au + ε,
u ~ N(0,Q(θ)⁻¹): via p(θ|y) ∝ p(y,u,θ)/p(u|y,θ) you run MCMC on θ only (funnel-free regardless of
dim u) then draw u|y,θ analytically. With a Gaussian likelihood linear in the latent, the NCP
conditional p(f̃|θ,y) is *exactly* Gaussian by conjugacy, so a Laplace-marginalizing sampler is exact —
but only for that combination; non-Gaussian likelihoods reintroduce genuine Laplace approximation error.
Two traps: divergences localized at small σ tempt a zero-avoiding prior on σ that does *not* remove them
(the pathology is the joint η–σ geometry, cured by marginalizing the field); and the inner L-BFGS mode
solve behind a Laplace marginal has a convergence *floor* (default maxiter=30 ≪ the O(κ/m)≈260 the
condition number demands), and silent non-convergence inflates the hyperparameter-level Hessian
18–54× and collapses the sampler step size up to 670×.

**Conditions.** Latent-GP / latent-Gaussian models, HMC/NUTS; NCP result demonstrated with fixed
hyperparameters and squared-exponential kernel; marginalization needs Q(θ) closed-form and a
Gaussian (exact) or Laplace-approximable (non-Gaussian) likelihood; float32 needs positive jitter
(~1e-4) in the Cholesky.

**Tier.** 🟢 established (subsumes `hmc-diagnostic-blindspots`,
`ncp-gp-cholesky-whitening-necessary-and-sufficient-diffuse-likelihood`,
`latent-gaussian-marginalization-strategy`, `merged-48` exact-Gaussian-conditional,
`worklog-laplace-marginal-lbfgs-convergence-floor`; plus supported `forum-c25` marginalize-latent-field).

**Sources.** [betanalpha:gaussian_processes](https://betanalpha.github.io/assets/case_studies/gaussian_processes.html) · [betanalpha:identifiability](https://betanalpha.github.io/assets/case_studies/identifiability.html) · [mc-stan:26425](https://discourse.mc-stan.org/t/help-reparameterize-gp-model-to-remove-divergent-transitions/26425) · [mc-stan:2172](https://discourse.mc-stan.org/t/algebraic-sovler-problems-laplace-approximation-in-stan/2172) ·
[mc-stan:6178](https://discourse.mc-stan.org/t/some-issues-that-may-arise-regarding-transformations/6178) · [pymc:16147](https://discourse.pymc.io/t/confusion-about-mcmc-and-inla/16147)

---

## Practical evidence — recs grounded in this claim

- [✓ E1](../../recs/gaussian-process/E1.md) — for a **GP funnel with divergences** → **pairs-plotting the ENSEMBLE of divergent draws** (with treedepth over
- [✓ E2](../../recs/gaussian-process/E2.md) — for **locating *where* a divergence occurred** → reporting the trajectory **endpoint weighted by error-increas
- [✗ E3](../../recs/gaussian-process/E3.md) — for a **geometric GP pathology** → cranking **adapt_delta / target_accept to an extreme** to suppress divergen
- [✓ G1](../../recs/gaussian-process/G1.md) — for a **latent-GP field** with fixed hyperparameters and a relatively **diffuse** likelihood → the **non-cente
- [✗ G2](../../recs/gaussian-process/G2.md) — for a **high-dimensional GP prior** → the **centered parameterization** does **NOT** work — and fails *invisib
- [✓ G3](../../recs/gaussian-process/G3.md) — for a **latent-Gaussian model** y = Au + ε, u ~ N(0,Q(θ)⁻¹) → **marginalize u and run MCMC only on θ** works (
- [✓/✗ G4](../../recs/gaussian-process/G4.md) — for a **GP regression** y ~ N(a+f, σ) with **divergences concentrated at small σ** → **marginalizing the laten
- [✓ G5](../../recs/gaussian-process/G5.md) — for an **NCP GP with a Gaussian likelihood linear in the latent** → a **Laplace-marginalizing sampler is exact
- [✗ G6](../../recs/gaussian-process/G6.md) — for a **Laplace marginal over a high-dimensional latent** → the **default L-BFGS maxiter=30** inner solve does
- [✓ G7](../../recs/gaussian-process/G7.md) — for that **high-dimensional Laplace inner solve** → raising **L-BFGS maxiter to ~300–500** works.
- [✗ G8](../../recs/gaussian-process/G8.md) — for **frozen/flat GP traces** with a collapsed step size → reading them as **successful acceptance** does **NO


## Related across the catalog

*Similar challenges in other model classes / computation areas (embedding neighbors):*

- [latent-factor C4 · For latent-Gaussian models, marginalizing the latent field strictly do](../../claims/latent-factor/C4.md) `0.85`
- [CC-geometry-sampling C5 · Likelihood-induced funnels have *no reparameterization analogue* — non](../../claims/CC-geometry-sampling/C5.md) `0.80`
- [✓ CC-geometry-sampling GP3 · when a **latent-Gaussian/GP regression shows divergences concentrated ](../../recs/CC-geometry-sampling/GP3.md) `0.80`
- [✗ latent-factor D1 · for a **latent-Gaussian** model → **joint HMC/NUTS on (u,θ)** without ](../../recs/latent-factor/D1.md) `0.79`
