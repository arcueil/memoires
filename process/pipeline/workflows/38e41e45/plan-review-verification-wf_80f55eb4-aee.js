export const meta = {
  name: 'plan-review-verification',
  description: 'Read-only verification sweep of suspected weaknesses in the post-adopt plan: apex staleness, reference integrity, doc contradictions, efficacy quick-wins, format/shipping tension, provenance gaps',
  phases: [{ title: 'Verify', detail: 'six independent checkers, read-only' }],
}
const ROOT = "/home/jp/rekursiv/bayesian-catalog"
const SCHEMA = {
  type:"object", required:["checker","findings"],
  properties:{ checker:{type:"string"},
    findings:{type:"array", items:{
      type:"object", required:["issue","evidence","severity","fix"],
      properties:{
        issue:{type:"string"},
        evidence:{type:"string", description:"the concrete verified fact (file, count, quote) — no speculation"},
        severity:{type:"string", enum:["high","medium","low"]},
        fix:{type:"string", description:"one-line proposed remedy"} } } } },
}
const CHECKS = [
 {k:'apex-staleness', p:`Check whether the apex layer is STALE after the corpus changed under it. Read ${ROOT}/dist/SUPER_AXIOMS.md (6 super-axioms, each with a "Subsumes:" list of C-labels, built when the corpus had 76 claims). The corpus has since changed: 4 claims were REMOVED as duplicates (see ${ROOT}/dist/MERGES_APPLIED.md), and 10 NEW claims were appended from pymc-L1 (they sit at the end of the Claims sections of these pages: CC-model-evaluation, CC-priors-identifiability, CC-geometry-sampling, regression, time-series-state-space, measurement-error-missing, sparse-shrinkage — recognizable by "pymc-labs" in their Tier/Sources lines). Verify: (a) does any SA "Subsumes" entry reference a claim that was removed/renumbered? (b) list the new pymc-derived claims NOT subsumed by any SA (orphans under the apex); (c) note that GAPS.md's apex-level gaps (iterative model-building, prior elicitation) may now have real sources — should the apex gain/extend an SA? Report concrete counts + names.`},
 {k:'ref-integrity', p:`Check internal reference integrity of the adopted corpus in ${ROOT}/dist/. Verify: (a) duplicate or colliding C-ids within each page in dist/target_sample/*.md (each page should have unique sequential-ish C-ids); (b) does dist/CLAIMS_SPINE.md's claim list match the pages exactly (count per page, titles)? (c) sample ~15 source short-ids from the NEW pymc-derived recs and claims (grep "pymc-labs" entries) plus ~10 from older recs (mc-stan:NNNN, pymc:NNNN, betanalpha:slug) — are the older ones resolvable against ${ROOT}/raw/ (raw/stan/NNNN.json etc.)? (d) do the "Tier." lines of merged pymc claims follow the same glyph convention as the rest? Report counts of broken/unresolvable references.`},
 {k:'doc-staleness', p:`Find documents that CONTRADICT the adopted state (adopted 2026-07-01: 6 super-axioms → 82 claims → 639 recs; rec-error reconciled to low-single-digits; pymc-L1 merged; see ${ROOT}/dist/README.md and the tail of ${ROOT}/QUIZ_STATE.md). Read and check: ${ROOT}/CHECKLIST_FOR_JP.md (likely still describes the old 446-claim audit-queue world), ${ROOT}/TARGET_ARCHITECTURE.md (says ~120 claims target), ${ROOT}/dist/REC_AUDIT.md (three audit rounds with contradictory error rates — does it have an authoritative final reconciliation?), ${ROOT}/dist/GAPS.md (updated for pymc-L1?), ${ROOT}/OPENSOURCE_PLAN.md (which parts are superseded by the pivot?), and whether the OLD product (${ROOT}/dist/claims/ = 446 entries, ${ROOT}/dist/FIELD_GUIDE.md) is anywhere marked as superseded/deprecated vs the new corpus — could an outside reader tell which is THE product? Report each stale doc with the specific contradicting statement.`},
 {k:'efficacy-quickwins', p:`Find efficacy slots we could fill TODAY from our own first-party experiments. Read ${ROOT}/experiments/RESULTS.md (the spike-slab vs horseshoe BlackJAX experiment: divergence counts etc.) and any other files in ${ROOT}/experiments/. Then scan ${ROOT}/dist/target_sample/sparse-shrinkage.md and mixture.md (and grep other pages for spike-slab/horseshoe recs) for recs whose efficacy is "pending" but which our experiment directly measures. Also count total efficacy slots vs pending across all pages in dist/target_sample/ (grep "efficacy:" and "pending"). Report: (a) the specific rec-ids our experiment can fill + the values, (b) the pending/filled totals, (c) the 3-5 highest-value recs that a cheap one-off BlackJAX experiment could ground next.`},
 {k:'format-tension', p:`Assess the FORMAT tension between the adopted corpus and the shipping plans. Read ${ROOT}/SCHEMA.md (per-entry YAML-frontmatter contract), ${ROOT}/OPENSOURCE_PLAN.md sections on the CI linter (§2f) and retrieval/indexing (§2g), the OKF/Obsidian items in ${ROOT}/CHECKLIST_FOR_JP.md (ecosystem alignment section), and ${ROOT}/validate_schema.py. The ADOPTED product is monolithic pages (${ROOT}/dist/target_sample/*.md) — multiple claims+recs per file, no per-entry frontmatter. Verify: (a) does validate_schema.py apply to anything in the adopted corpus? (b) what exactly do the OKF/Obsidian plans assume (per-entry files? wikilinks? frontmatter fields)? (c) is there any per-entry rendering of the new corpus, or would OKF/Obsidian require building one? (d) how does the SCHEMA's contradicts_ids / uses_techniques linking surface in the page format, if at all? Report the concrete mismatches — this decides ship-pages vs render-per-entry.`},
 {k:'provenance-repro', p:`Check whether an outside reader could trace HOW the adopted corpus was built (the 446-granular → 82-claim/639-rec pipeline). Look in ${ROOT} for: any script or doc that generates dist/target_sample/*.md (the generation actually happened via Claude workflow scripts stored OUTSIDE the repo in a session directory — verify nothing in-repo reproduces it); whether ${ROOT}/dist/README.md or TARGET_ARCHITECTURE.md documents the build lineage + review bounds methodology; whether the audit trail (REC_AUDIT.md, MOVE_AUDIT.md, MERGES_APPLIED.md, pymc_L1/README.md) forms a coherent provenance story; and whether the repo is a git repository (run: git -C ${ROOT} rev-parse --git-dir 2>&1 — expect failure). Also check ${ROOT}/catalog/_intermediate/ for the key intermediates (taxonomy_assignments.json, apex_analysis.json, pymc_findings.json) — are they referenced from any doc? Report what provenance is missing for an open-source release.`},
]
const results = await parallel(CHECKS.map(c => () => agent(
`READ-ONLY verification for a plan review — do NOT edit any file. ${c.p}\n\nBe concrete: verify every claim you make by reading the actual files (quote/count). If a suspicion turns out FALSE, say so explicitly (a clean bill is as valuable as a finding). Return {checker:"${c.k}", findings:[{issue, evidence, severity, fix}]}. One-line summary.`,
  { label:`chk:${c.k}`, phase:"Verify", schema:SCHEMA }
)))
const all=results.filter(Boolean)
return { checkers:all.length, total:all.flatMap(r=>r.findings).length,
  high:all.flatMap(r=>r.findings).filter(f=>f.severity==='high').length, results:all }
